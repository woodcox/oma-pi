#!/bin/bash

# Reinstalls PimaTerm with missing packages and default configs.

if gum confirm "Refresh PimaTerm (including overwriting configs)?"; then
  curl -fsSL https://raw.githubusercontent.com/woodcox/pimaterm/install | bash
fi
