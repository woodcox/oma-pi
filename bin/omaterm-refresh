#!/bin/bash

# Reinstalls PiTerm with missing packages and default configs.

if gum confirm "Refresh PiTerm (including overwriting configs)?"; then
  curl -fsSL https://raw.githubusercontent.com/woodcox/piterm/install | bash
fi
